/// Shared product identity constants.
///
/// Centralized so user-facing surfaces that must name the app (OAuth
/// consent screens, dialogs, about copy) stay consistent instead of
/// scattering string literals.
export const PRODUCT_NAME = 'Dafman';
