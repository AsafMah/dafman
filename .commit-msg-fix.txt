fix(20b regression): restore ensureDefaultWorkspace

knip flagged this as unused in 20b and I deleted it. The consumer
in src-bun/index.ts (one-time default-workspace backfill on first
launch) was missed because knip's reachability graph doesn't trace
through electrobun's bun-side build entry. vue-tsc only covers
src/ and bun test only compiles src-bun/__tests__/, so neither
gate caught the broken import — it only surfaces in
`electrobun dev --watch` or `electrobun build`, both downstream
of `bun run check` in different ways. (`bun run build` does run
electrobun build, but apparently 20b's check passed; possibly the
electrobun build emits a warning rather than failing on this in
some configs. TODO: tighten the bun-side lint surface.)

Function body restored exactly as it was pre-20b:
- Resolves to <homedir>/dafman
- Creates the directory on demand (mkdir recursive)
- Returns "" on any failure (homedir missing, mkdir EACCES, etc.)
  so the consumer's null-check path handles it gracefully

Also includes the D4 tech-debt entry (TypeScript-Go future
experiment) added earlier in this session — both fit the same
"tech-debt accuracy" theme.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
