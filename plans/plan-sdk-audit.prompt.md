# Plan — SDK + CLI feature audit (2026-05-22)

> Authoritative cross-reference of every surface exposed by
> `copilot-sdk-supercharged` and the bundled `@github/copilot` CLI
> against what Dafman currently implements. Generated by reading the
> SDK's `README.md` end-to-end (lines 1–1135), `dist/generated/rpc.d.ts`
> (~2400 lines), and the CLI's `changelog.json` (212 versions,
> 436 "added" entries).
>
> Use this doc when planning the next Phase. STATUS.md tracks "what
> shipped"; this doc tracks "what the platform offers and we haven't
> wired yet".

---

## Methodology

1. **Supercharged README** — every config option, every `client.*` /
   `session.*` method, every event, every handler.
2. **`dist/generated/rpc.d.ts`** — every RPC method on both the
   server-scoped (`createServerRpc`) and session-scoped
   (`createSessionRpc`) surface.
3. **`@github/copilot` CHANGELOG** (`changelog.json`) — features
   shipped by the CLI itself that surface to clients through one of
   the SDK channels above. Skimmed every "added" entry from v0.0.329
   through v1.0.48.
4. **Cross-reference** against `src-bun/app/` and `src/` for what's
   actually wired today.

---

## A. Wire-ready surfaces we don't expose yet (high ROI)

These are RPCs the SDK already wraps; we just need UI + a thin
session-store action. Sorted by approximate user-visible value.

| Surface | SDK call | Effort | Notes |
|---|---|---|---|
| **Skill library** (list + enable/disable + reload + setDisabledSkills) | `rpc.skills.list/enable/disable/reload`, `rpc.skills.config.setDisabledSkills` | 1–2 d | We have a per-session enable toggle. No library UI / no global default-disabled list. |
| **MCP server registry** (config CRUD + enable/disable per-session + reload) | `rpc.mcp.config.add/update/remove/enable/disable`, `rpc.mcp.enable/disable/reload`, `rpc.mcp.discover` | 3–4 d | Phase 6 in STATUS.md but no `McpManager.vue` yet. CLI `1.0.21` adds `copilot mcp` CLI command for same — we can match. |
| **MCP OAuth login flow** | `rpc.mcp.oauth.login`, URL elicitation, `client_credentials` grant in 1.0.40 | 1 d | Today URL elicitations show the browser link via PendingRequestCard but we don't surface MCP-specific status (auth required / auth complete) toasts. |
| **Custom agent picker** (per-session + reload after editing on disk) | `rpc.agent.list/getCurrent/select/deselect/reload` | 1 d | Gear popover entry; mirrors model picker. SDK `customAgents` config sets directories. |
| **Plans API rendering** | `rpc.plan.read/update/delete` | 1 d | Collapsible "Plan" panel inside the chat tab. CLI's `/diff`-style review of the plan in a side rail. |
| **Background tasks / fleet** | `rpc.tasks.startAgent/list/promoteToBackground/cancel/remove`, `rpc.fleet.start` | 3 d | Edge panel listing running background agents. CLI ships `/tasks`, `/fleet`, `/delegate`, `/autopilot`, `/fork`. We have none. |
| **History compaction + truncate** | `rpc.history.compact/truncate` | 0.5 d | Already half-wired via gear popover (compact). Truncate-to-message not wired. Pairs with `/rewind`. |
| **Usage metrics dashboard** | `rpc.usage.getMetrics` | 1 d | We surface usage in a footer pill; not a full Settings → Usage view with the per-model + per-day rollup that CLI ships in `1.0.32` (75/90% warnings) and `1.0.35` (contribution graph). |
| **Account quota** | `rpc.account.getQuota` | 0.5 d | Show in Settings → Account; warn on threshold. |
| **Available tools list grouped by source** | `rpc.tools.list` | 1 d | Pairs with the per-session allow/exclude UI. CLI ships `tools.list` RPC in `0.0.407`. |
| **Per-session tool allow/exclude UI** | SDK `availableTools` / `excludedTools` config; CLI `--available-tools` / `--excluded-tools` in 0.0.370 | 1 d | Already on STATUS Phase 4. |
| **Custom agent / skills config discovery toggle** | `customAgents` / `skillDirectories` / `disabledSkills` config | 0.5 d | We flipped `enableConfigDiscovery: true` already. Settings → "Skill / agent search paths" editor would let the user add `~/.copilot/skills` etc. explicitly. |
| **Plugin list** (read-only) | `rpc.plugins.list` | 0.5 d | Useful to show in Settings → Diagnostics so the user knows what got auto-loaded. |
| **Extension list + enable/disable** | `rpc.extensions.list/enable/disable/reload` | 1 d | CLI `0.0.5` added `/extensions`. We don't surface. |
| **SDK-registered slash commands** | `commands` config + `rpc.commands.handlePendingCommand` + CLI `1.0.10` (clients register slash commands) | 1 d | Local `SlashCommandPlugin` only knows about `/cd` etc. We can route to SDK commands. CLI also adds `/skill-name` invocation in `0.0.389`. |
| **Triggerable elicitation (already shipped)** | `session.ui.elicitation` | DONE | Phase 15. |
| **Plan-mode + autopilot toggle** | CLI 0.0.387 (plan mode), 0.0.400 (autopilot), 0.0.411 (SDK APIs for plan mode/autopilot/fleet) | 1 d | We have a mode pill in the composer (`ModeButtonGroup`) — does it actually wire to `rpc.mode.set` for plan/autopilot/yolo? **Audit action**: verify; if not, wire it. |
| **`session.shell.exec` + `kill` streaming** | `rpc.shell.exec/kill` | 1 d | We don't run shell from the renderer side; could power a built-in terminal panel (cf. the new Copilot app's "Terminal" tab from Phase 17). |
| **Workspace files** (read/list/create scoped to session workspace) | `rpc.workspaces.getWorkspace/listFiles/readFile/createFile` | 1 d | Power a per-session "files this session has touched" panel — Phase 17 anti-takeaway item E becomes cheap. |
| **Session metadata fetch** | `client.getSessionMetadata(id)` | 0.5 d | Today we only know what's in our own store. Refreshing model/title/createdAt from disk on resume is more correct. |
| **`client.on()` lifecycle events** | `session.created` / `deleted` / `updated` / `foreground` / `background`, `capabilities.changed` | 0.5 d | Live-update Sessions Manager without polling. Also: react to capabilities changes (e.g. another client connects with elicitation). |
| **Resume with `continuePendingWork: true`** | `client.resumeSession({ continuePendingWork })` | 0.5 d | When resuming a session that ended mid-turn, automatically continue. |
| **Set approve-all per session via RPC** | `rpc.permissions.setApproveAll` + `resetSessionApprovals` | 0.5 d | We have an in-memory `approveAllBySession` map; SDK can persist + reset. Pairs with `/allow-all` / `/yolo` CLI commands. |

---

## B. Config knobs not yet set on `baseSessionConfig`

Stuff `src-bun/app/sessions.ts` could plumb through with very little code.

| Knob | Type | Today | Should be |
|---|---|---|---|
| `infiniteSessions.{enabled, backgroundCompactionThreshold, bufferExhaustionThreshold}` | object | default (enabled, 80%/95%) | Settings → Conversation: tuneable thresholds. |
| `sessionIdleTimeoutSeconds` (client-level) | number | unset | Settings → Conversation; matches SDK + CLI behavior. |
| `systemMessage.{mode: append|customize|replace, sections}` | object | unset (default append) | Settings → System prompt; with section editor for `customize`. |
| `provider.{type, baseUrl, apiKey, bearerToken, wireApi, azure.apiVersion}` | object | unset | Settings → Custom provider (Ollama / Azure / OpenAI-compat / Anthropic). CLI ships `COPILOT_PROVIDER_BASE_URL` env var in `0.0.370-7`. |
| `gitHubToken` (session-level) | string | unset | Per-session GitHub account pin. Phase 5 multi-account. |
| `includeSubAgentStreamingEvents` | boolean | unset | When sub-agents land, we want the deltas. |
| `streaming` | boolean | default true | Settings toggle (debug). |
| `disabledSkills: string[]` | array | unset | Mirrors `setDisabledSkills` RPC; useful at create-time. |
| `mcpServers` / `skillDirectories` / `customAgents` (explicit lists) | array | unset (rely on discovery) | Project-level overrides once Projects land. |
| `hooks.{onPreToolUse, onPostToolUse, onUserPromptSubmitted, onSessionStart, onSessionEnd, onErrorOccurred}` | callbacks | unset | Most useful: `onUserPromptSubmitted` to inject project context; `onSessionStart` for telemetry. |
| `telemetry.{otlpEndpoint, filePath, exporterType, sourceName, captureContent}` | object | unset | Phase 11 — Settings → Privacy → Telemetry. CLI ships `/help monitoring`. |
| `onGetTraceContext` callback | callback | unset | Nest our spans inside CLI spans for end-to-end traces. |
| `cliUrl` / `port` / `useStdio` (client) | string/number/bool | unset (spawn local) | Connect to an already-running CLI server (e.g. dev container). CLI ships `--connect <id>` in `1.0.32`. |
| `Symbol.asyncDispose` on session | API | unused | Switch to `await using session = …` in places we already have try/finally cleanup. |

---

## C. CLI features we haven't surfaced (from changelog)

CLI shipped these; many are usable through the SDK by exposing the
RPC or by implementing them in our renderer.

### High value
- **`/fork` session** (CLI 1.0.45) — `rpc.sessions.fork`. We can ship a
  gear-popover entry "Fork this session" that opens a new tab with the
  branched history.
- **`/rewind` + double-Esc timeline** (CLI 1.0.13) — pairs with
  `rpc.history.truncate`. Right-click a message → "Rewind to here".
- **`/undo` last turn + revert file changes** (CLI 1.0.10).
- **`/diff` session changes** (CLI 0.0.389, syntax-highlighted in 1.0.5).
  Phase 10 in STATUS but the SDK has `rpc.workspaces.listFiles` we can
  use today.
- **`/usage` with contribution graph** (CLI 1.0.35) — read from
  `rpc.usage.getMetrics`.
- **`/share html` self-contained HTML export** (CLI 1.0.15) —
  complements our Markdown / JSON export.
- **`#issue / #PR` autocomplete** (CLI 0.0.420). MentionPlugin
  extension; needs GitHub auth + repo context.
- **Background task notifications** (CLI 0.0.422) — auto-notify when a
  background shell command or agent completes. We notify on turn end
  but not on background-tool completion.
- **`/research` deep research** (CLI 0.0.417).
- **`/init` Copilot instructions generator** (CLI 0.0.396).
- **`/review` code review** (CLI 0.0.388).
- **`/delegate` to Copilot coding agent** (CLI 0.0.353) — connects to
  remote GitHub-hosted agent. SDK exposes via tasks API in 1.0.41.
- **Cross-session memory** (CLI 0.0.412, experimental) — ask about
  past work / files / PRs across sessions. Needs the Memory backend in
  STATUS Phase 8.
- **`/restart` hot restart preserving session** (CLI 1.0.3).
- **`/instructions` toggle picker** (CLI 0.0.407).
- **`/statusline` / `/footer`** (CLI 1.0.30) — configurable status-bar
  items (dir, branch, effort, ctx window, quota). Our chat-usage footer
  is close.
- **Statusline "changes" toggle** (CLI 1.0.36) — added/removed lines this
  session. Phase 17 takeaway #5 already proposed this.
- **`/changelog` viewer** (CLI 0.0.406).
- **Skills as slash commands** (CLI 0.0.389) — `/skill-name` invokes a
  skill directly. Mid-input slash + multiple skills per message
  (CLI 1.0.44).
- **Multiple custom agents per session via `/agent`** (CLI 0.0.353).
- **`startup prompt` hooks to auto-submit prompts on session start** (CLI 0.0.422).
- **Plugin marketplace** (CLI 0.0.392, `/plugin`) — full registry. We
  don't expose plugins at all today.
- **MCP `outputSchema` structured content** (CLI 0.0.401) — we already
  have `JsonValueView` (Phase 14) that's structure-driven.
- **MCP server sampling (LLM inference) with user approval** (CLI 1.0.13).
- **Allow read-only `gh` CLI commands auto-approve** (CLI 1.0.46) —
  baked permission rule pattern we can copy.
- **Auto-detect Azure DevOps repos, disable github-mcp-server** (CLI 1.0.40).
- **Theme picker with colorblind variants** (CLI 0.0.400, 0.0.407).
- **Permanent permission rules per location** (CLI 0.0.407) — we have
  the rule editor; persistence across sessions is the gap.

### Medium value
- **`/export`, `/copy`, `/share` aliases** (CLI 1.0.33, 0.0.422).
- **`@/abs`, `@~/`, `@../` paths** (CLI 1.0.5) — extend MentionPlugin
  beyond the workspace root.
- **`#` for GitHub issues / PRs / discussions** (CLI 0.0.420).
- **MCP servers from Windows On-Device Registry** (CLI 0.0.411).
- **`.devcontainer/devcontainer.json` MCP discovery** (CLI 1.0.3).
- **Repo-level config: `.github/copilot/config.json`** (CLI 0.0.421) —
  shared project settings (marketplaces, launch messages).
- **Plugin LSP server bundles** (CLI 0.0.405) — would require Phase 8
  LSP work.
- **`USE_BUILTIN_RIPGREP` + bundled ripgrep** (CLI 0.0.359). Bun-side
  `grep`/`glob` tools if we extend our built-ins.

### Probably skip (TUI-specific or low value for us)
- Alt-screen buffer, mouse selection, `Ctrl+R` history search,
  `Ctrl+x b` to background, j/k diff nav, terminal cursor positioning,
  `--alt-screen`, `--mouse`, `/streamer-mode`, status-bar tweaks
  specific to terminal rendering.
- `pwsh`-shim fixes, `BASH_ENV`, `ctrl+x ctrl+e` external editor —
  irrelevant; we have a real composer.

---

## D. Truly new surfaces not previously in our plans

Concepts we hadn't considered before.

1. **`rpc.workspaces.*` (session-scoped workspace files API)** — a per-
   session file area with `listFiles` / `readFile` / `createFile`. The
   CLI uses this for plans (`plan.md`), checkpoints, paste-overflow
   spill (CLI 0.0.397: >30 KB clipboard pastes spill to workspace
   files). A "Files" tab on the chat could surface this directly.
2. **`rpc.commands.handlePendingCommand`** — the CLI can defer a slash
   command back to the *client* to handle. Lets the agent emit a
   command we render natively (e.g. "open the diff for these files").
3. **MCP server *instructions*** (CLI 0.0.400) — MCP servers can carry
   instructions that get injected into the system message. We should
   render these in the MCP registry detail panel.
4. **`subagentStart` hook** (CLI 1.0.7) — inject context into a sub-
   agent's prompt. Used by Critic / Rubber-duck.
5. **`preCompact` hook** (CLI 1.0.5) — run a command before context
   compaction. Could let us inject a "save plan to disk" step.
6. **`agentStop` / `subagentStop` hooks** (CLI 0.0.401, 1.0.45).
7. **`PermissionRequest` hook** (CLI 1.0.16) — programmatic
   approve/deny based on rules. Could power a "remembered permission"
   layer that runs *before* our PendingRequestCard.
8. **`postToolUseFailure` hook** (CLI 1.0.15) — only fires on tool
   failure. Could power an automatic "retry with different args" loop.
9. **Tool search (Claude only)** (CLI 1.0.6) — model discovers and uses
   tools dynamically per turn. Implications for our `availableTools`
   UI (it's hint, not hard cap).
10. **HTTP hooks** (CLI 1.0.35) — hook POSTs JSON to a URL instead of
    running a local command. Could power webhook integrations
    (Phase 9 automations) without a scheduler.
11. **OTel "GenAI" semantic conventions** (CLI 1.0.45) — tool spans +
    `gen_ai.client.operation.duration` metric. Phase 11 telemetry
    surface is now well-defined.
12. **Plugin hook env vars** (`PLUGIN_ROOT`, `COPILOT_PLUGIN_ROOT`,
    `CLAUDE_PROJECT_DIR`, `CLAUDE_PLUGIN_DATA`) — when we eventually
    do plugin support, pre-canned env contract.

---

## E. Re-evaluate prior deferrals (NEW evidence)

| Deferral | Original reason | New evidence | Verdict |
|---|---|---|---|
| **Image generation** | CLI `app.js` didn't grep for `responseFormat` | SDK has `assistant.image` + `assistant.content` events per README; CLI 0.0.333 ships image *input* (`@`-mention); image *output* path is via models that support it natively (Claude) and emerges through `content` events. | **Pick up in Phase 3.** Wire `responseFormat: "image"` send option + `assistant.image` event handler + base64 renderer. The earlier "no responseFormat strings in app.js" search was misleading — the model controls the format, not the CLI. |
| **Approve-for-session rule editor** | SDK rejected minimal shape | SDK's `setApproveAll` + `resetSessionApprovals` RPCs are the correct surface. Per-kind rule body required. | **Pick up after Permission audit.** Add Settings → Remembered Permissions with full per-kind rule editor + clear button. |
| **Tier-2 E2E** | Cross-platform debug ports | Same. Still 2 d. Worth scheduling now that audit is done. | Keep deferred. |
| **Tool toggle UI** | "No documented runtime SDK mutation" | Confirmed — needs session restart to apply `availableTools` change. Ship as create-time + Settings default. | **Pick up.** Static config + restart-to-apply hint is acceptable. |

---

## F. Proposed phase ordering (replaces "next concrete steps" 1–4)

Each item is a single PR-ish chunk. Effort = focused engineering days.

### Phase 18 (next session) — Skills + MCP foundation (~3 d)
1. **Skill library UI** (1 d) — `rpc.skills.list/enable/disable/reload`
   + `setDisabledSkills`. Settings → Skills tab with per-skill detail
   panel (description, source path, scope).
2. **MCP registry MVP** (1.5 d) — `rpc.mcp.config.list/add/update/remove`
   + per-session enable/disable + reload. Settings → MCP tab.
   Server detail shows tools + instructions (CLI 0.0.400).
3. **MCP OAuth status toast** (0.5 d) — wire URL elicitation to a
   top-right notification with status updates.

### Phase 19 — Custom agents + tasks/fleet (~3 d)
1. **Agent picker** in gear popover (1 d) — `rpc.agent.*` + `customAgents`.
2. **Tasks panel** edge group (1 d) — `rpc.tasks.startAgent/list/cancel`.
3. **Fleet runner** (1 d) — `rpc.fleet.start` with concurrency control;
   surfaces sub-agents.

### Phase 20 — Power UX (~2 d)
1. **Per-session tool allow/exclude UI** + `rpc.tools.list` grouped by
   source (0.5 d).
2. **Plans panel** in chat tab via `rpc.plan.*` (0.5 d).
3. **Usage dashboard** (Settings → Usage) via `rpc.usage.getMetrics` +
   `rpc.account.getQuota` (0.5 d).
4. **Fork session** entry in gear popover via `rpc.sessions.fork` (0.5 d).

### Phase 21 — Image generation + remembered permissions (~2 d)
1. **Image generation** (1 d) — `responseFormat: "image"` composer
   option + `assistant.image` event + image renderer.
2. **Settings → Remembered Permissions** (1 d) — `setApproveAll` +
   `resetSessionApprovals` + per-kind rule editor reuse from
   `PermissionRuleEditor.vue`.

### Phase 22 — Multi-account + provider/BYOK (~3 d)
1. **Accounts UI + OS-keyring storage** (1.5 d).
2. **Per-session GitHub token pin** (`gitHubToken` config) (0.5 d).
3. **Custom provider settings** (1 d) — `provider.{type, baseUrl,
   apiKey, …}` editor + Ollama / Azure / OpenAI-compat presets.

### Phase 23 — Plans rendering, history, lifecycle live updates (~2 d)
1. **`client.on()` live lifecycle events** for Sessions Manager (0.5 d).
2. **History `truncate` → "Rewind to here"** message-action (0.5 d).
3. **`continuePendingWork: true` on resume** when a session ended mid-
   turn (0.5 d).
4. **Session metadata refresh on resume** via `getSessionMetadata`
   (0.5 d).

### Later (defer)
- Phase 9 automations (scheduler, webhooks, HTTP hooks).
- Phase 10 editor (diff viewer + accept/reject hunks).
- Phase 11 browser + telemetry + docs.
- Workspace files surface (Phase 24 candidate).
- Memory backend (Phase 25).
- Plugin / extension UI (Phase 26).

---

## G. Open questions

1. **Tool search (Claude only).** If a model dynamically discovers tools
   per turn, our `availableTools` UI is a *hint*, not a hard guarantee.
   Document this prominently in the tool toggle popover.
2. **Background tasks vs streaming.** `rpc.tasks` runs an agent in the
   background — does the streaming event channel still deliver
   `task_complete` for those? Verify before wiring the tasks panel.
3. **`session.foreground` / `background` semantics.** Server-mode
   only or available to embedded clients? Affects how we wire the
   Sessions Manager.
4. **`requestElicitation` capability negotiation.** Multi-client: if
   another client (e.g. a VS Code ACP host) joins our session, does
   our elicitation card win? Need to test with multi-client.
5. **`responseFormat: "image"`** — confirm via empirical test that the
   string is `"image"` not e.g. `"png"`/`"image_base64"`.

---

## H. Decision log

- **Image gen un-deferred.** Per re-evaluation in §E.
- **MCP registry promoted ahead of Projects.** Highest user-value
  per day of work; doesn't depend on Projects landing.
- **OTel telemetry deferred to Phase 11 still** — design space too
  open without a target backend.
- **Plugin / extension UI deferred** — low signal until the MCP and
  Agents work surfaces enough plugin chrome to design around.
- **Per-session GitHub token + custom providers grouped together** —
  both are auth/identity per-session; one Settings → Accounts pane.

---

## I. Reading order for the next agent

1. **This file** (you are here).
2. `node_modules/copilot-sdk-supercharged/README.md` — full reference;
   we read every line for this audit.
3. `node_modules/copilot-sdk-supercharged/dist/generated/rpc.d.ts` —
   complete typed RPC surface; cross-reference §A items.
4. `node_modules/copilot-sdk-supercharged/dist/types.d.ts` — event
   union, permission union, hook signatures.
5. `node_modules/@github/copilot/changelog.json` — sanity-check
   against any feature you're considering ("did this already ship
   upstream?").
6. `STATUS.md` — current state; this audit is layered on top.
